// Goal Create Dto enums
export enum MeasurementUnits {
  LBS,
  KGS,
}

export enum DistanceUnits {
  MI,
  KM,
}

export enum Status {
  NOT_STARTED = 'NOT_STARTED',
  IN_PROGRESS = 'IN_PROGRESS',
  DEFAULT = NOT_STARTED,
}

// Auth Sign Up Enum
export enum unitPref {
  IMPERIAL = 'IMPERIAL',
  METRIC = 'METRIC',
}
